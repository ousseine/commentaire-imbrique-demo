{% extends 'base.html.twig' %}

{% block title %}New Comment{% endblock %}

{% block body %}

    <div id="admin" class="content admin-comment">
        <div class="top-title">
            <h1 class="title">Nouveau commentaire</h1>

            <div class="top">
                <a class="btn btn-sm btn-dark" href="{{ path('app_admin_comment_index') }}">Retour</a>
            </div>
        </div>

        <div class="contain">
            {{ include('admin/comment/_form.html.twig') }}
        </div>
    </div>

{% endblock %}
