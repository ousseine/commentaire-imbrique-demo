{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}

    <div id="admin" class="content admin-comment">
        <div class="top-title">
            <h1 class="title">Editer le commentaire #{{ comment.id }}</h1>

            <div class="top">
                <a class="btn btn-sm btn-dark me-1" href="{{ path('app_admin_comment_index') }}">Retour</a>
                {{ include('admin/comment/_delete_form.html.twig') }}
            </div>
        </div>

        <div class="contain">
            {{ include('admin/comment/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>

{% endblock %}
