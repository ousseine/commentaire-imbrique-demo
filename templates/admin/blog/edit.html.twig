{% extends 'base.html.twig' %}

{% block title %}Admin : Edit {{ post.title }}{% endblock %}

{% block body %}

    <div id="admin-blog" class="content">
        <div class="top-title">
            <h1 class="title">Editer cet article</h1>

            <div class="top">
                <a class="btn btn-sm btn-dark me-1" href="{{ path('app_admin_blog_index') }}">Retour</a>
                {{ include('admin/blog/_delete_form.html.twig') }}
            </div>
        </div>

        <div class="contain">
            {{ include('admin/blog/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>

{% endblock %}
